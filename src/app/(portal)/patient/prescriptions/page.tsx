import { PrescriptionsContent } from "./_content";

// Data is fetched client-side (signed URLs are short-lived and must be fresh on each visit).
export default function PrescriptionsPage() {
    return <PrescriptionsContent />;
}
