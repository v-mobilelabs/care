rules_version = '2';
// All Firestore access is performed server-side via the Firebase Admin SDK.
// Client-side direct access is intentionally disabled.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
